/*

  PRODUCTION configuration file for the node application
  =======================================================

*/
module.exports = {
  // All settings come from Heroku, no need to set them manually
};
