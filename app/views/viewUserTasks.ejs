<!DOCTYPE html>

<html lang="en">
<% include components/htmlhead %>

<body>
    <% include components/navbar %>
    <div class="container">
        <% if (showAssignedBy) { %>
            <% include viewTasksAssignedByUser %>
        <% } %>
        <% if (showAssignedTo) { %>
            <% include viewTasksAssignedToUser %>
        <% } %>
        </div> <!-- container -->
    <% include components/scripts %>
</body>
</html>
